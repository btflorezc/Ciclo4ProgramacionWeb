/* import PropTypes from 'prop-types'

const Dependiente = (detalleProyecto) => {
    console.log(detalleProyecto)
    return <h1>Soy el dependiente de Proyecto</h1>
}
Dependiente.propTypes ={
    detalleProyecto : PropTypes.number.isRequired
}

export default Dependiente */